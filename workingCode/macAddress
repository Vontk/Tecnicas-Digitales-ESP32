#include <WiFi.h>

void setup() {
  Serial.begin(115200);
  // Iniciar WiFi
  WiFi.mode(WIFI_STA);
  WiFi.begin();
  Serial.print("Dirección MAC del ESP32: ");
  Serial.println(WiFi.macAddress());
}

void loop() {
  // Nada que hacer aquí
}
